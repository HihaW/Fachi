const wikiDatabase = [
    {
        fandomName: "Hoyoverse",
        categoryId: "hoyoverse",
        characters: [
            {
                name: "Firefly",
                initials: "FF",
                image: "img/firefly.webp", 
                origin: "Honkai: Star Rail",
                birthday: "Unknown",
                faction: "Stellaron Hunters",
                summary: "A member of the Stellaron Hunters who fights clad in a mechanical armor known as 'SAM.' Born as a weapon, she seeks to defy her engineered fate and live simply as a human.",
                story: "Constructed originally as a biological weapon tasked with confronting the devastating Swarm on behalf of Glamoth, Firefly was modified genetically, a condition that left her bound to Entropy Loss Syndrome. Compelled to reside inside the heavy armor known as SAM to sustain her life, she eventually joined the Stellaron Hunters. Seeking to defy her predetermined genetic blueprint, her absolute journey remains focused on uncovering a simple way to experience existence, eventually finding meaning as a human.",
                youtubeId: "https://www.youtube.com/watch?v=eFXQqY3ia6k"
            },
            {
                name: "Furina",
                initials: "FR",
                image: "img/furina.jpg", 
                origin: "Genshin Impact",
                birthday: "October 13",
                faction: "Fontaine",
                summary: "The former Hydro Archon of Fontaine, celebrated for her theatrical flair and dramatic public persona. Behind the stage lights lies a history of immense sacrifice and quiet endurance.",
                story: "For five centuries, Furina acted as the public figurehead and Hydro Archon of Fontaine, maintaining an energetic, dramatic face to outwit the prophecy of ruin that loomed over her homeland. Behind this public performance sat a regular human enduring heavy isolation and intense emotional burdens. Having relinquished her divine status, she has stepped off the literal stage to appreciate a quieter mortal path, rediscovering her love for theatrical performances on her own terms.",
                youtubeId: "https://www.youtube.com/watch?v=EN79SfbcvIE"
            }
        ]
    },
    {
        fandomName: "Limbus Company",
        categoryId: "Limbus Company",
        characters: [
            {
                name: "Rien",
                initials: "RN",
                image: "img/rien.png", 
                origin: "Limbus Company",
                birthday: "Unknown",
                faction: "The Index (House Of Spiders)",
                summary: "Rien is a tragic, tormented figure ordered to act as an exact psychological and physical imitation of Roland. Enslaved by the Prescripts, he lived a controlled life where every action, speech, and even his combat choices were dictated for him.",
                story: "Though forced to let his own wife and daughter die to maintain his role, Rien secretly developed genuine attachments. During the events of Canto 9, he is hunted for disobeying the Prescripts in favor of his own hidden desires. In his final moments, instead of reciting a forced prayer, he manages one final act of pathetic but profound defiance by taking his own life.",
                youtubeId: "https://www.youtube.com/watch?v=WF5wvJjEwVU"
            },
            {
                name: "Sora",
                initials: "SR",
                image: "img/sora.webp", 
                origin: "Limbus Company",
                birthday: "Unknown",
                faction: "The Index (House Of Spiders)",
                summary: "An anxious, deeply insecure apprentice Proxy who blindly relies on the Index's to Prescripts function.",
                story: "Sora masks her brutal nature behind a timid, fragile facade to trick her targets. Seeking validation from her surrogate father, Rien, her extreme psychological turmoil triggers her Effloresced E.G.O: Procuration (heavy steel shackles). She aggressively executes the Index's violent mandates, entirely incapable of separating her own morals from the transactional commands of a Prescript.",
                youtubeId: "https://www.youtube.com/watch?v=Y2-VkdfA2os"
            }
        ]
    },
    {
        fandomName: "Arknight Endfield",
        categoryId: "arknight-endfield",
        characters: [
            {
                name: "Laevatain",
                initials: "LN",
                image: "img/lae.webp",
                origin: "Arknight",
                birthday: "Unknown",
                faction: "Rhodes Island",
                summary: "A powerful Sarkaz operator specializing in devastating Heat-element Originium Arts. Though she initially gives off the impression of an aggressive, fiery girl, she is actually an excellent listener who receives highly positive feedback from her peers for her exceptional teamwork and communication skills.",
                story: "Authorized by Warfarin, she spent years executing a dangerous, solo field mission in the extreme conditions of the north. Bearing a striking connection to the legendary operator Surtr, her story follows her journey through fading memories and illusions in the Woods of Memories, where she ultimately learns to channel her incinerating flames into a direct path forward.",
                youtubeId: "https://www.youtube.com/watch?v=8Dqg0k0vbAQ&list=RD8Dqg0k0vbAQ&start_radio=1",
            },
            {
                name: "Zhuang Fangyi",
                initials: "ZF",
                image: "img/fangyi.webp",
                origin: "Arknight Endfield",
                birthday: "Unknown",
                faction:  "Wuling Scientific Institute",
                summary: "An incredibly powerful 6-star Electric Striker who serves as both the Viceroy and a Tianshi of the region of Wuling. On the surface, she is a deeply focused and formidable leader, but she also shares a distinct, uncanny physical resemblance to Lungmen's mythical Sui siblings.",
                story: "Before losing her memories to the planet's hardships, she was a dedicated Tianshi Intern who worked closely alongside the Endministrator. Now navigating the present day without her past, she embraces her current life with a peaceful philosophy, choosing to protect her people with her lightning-charged Sunderblades while finding simple joy in a shared drink with her peers.",
                youtubeId: "https://www.youtube.com/watch?v=e0N-S1FSc8Y&list=RDe0N-S1FSc8Y&start_radio=1",
            }
        ]
    },
    {
        fandomName: "Wuthering Waves",
        categoryId: "wuthering-waves",
        characters: [
            {
                name: "Zani",
                initials: "ZI",
                image: "img/zani.webp",
                origin: "Wuthering Waves",
                birthday: "November 21",
                faction: "Montelli Family",
                summary: "A meticulous and highly dependable Spectro Mutant Resonator who works for the prominent Montelli family. Outfitted in a sharp office uniform with a black overcoat, she values order, takes immense pride in her strict daily routine, and treats her dark under-eye circles like a hard-earned badge of honor.",
                story: "Despite her deeply ingrained preference for structure, she constantly battles an intense internal drive to jump straight into immediate, protective action. She works around the clock dreaming of a secure and peaceful Ragunna where citizens can live undisturbed, ultimately finding a literal burst of color and a new path forward when she meets the Rover.",
                youtubeId: "https://www.youtube.com/watch?v=6tZfkFCz6qI",
            },
            {
                name: "Zhezhi",
                initials: "ZI",
                image: "img/zhezhi.webp",
                origin: "Wuthering Waves",
                birthday: "December 31",
                faction: "Huanglong",
                summary: "A quiet, shy, and socially awkward Glacio Congenital Resonator who works as a commissioned painter. Though she is far from eloquent and easily becomes flustered, her profound dedication, meticulous nature, and fierce love for her art speak volumes.",
                story: "Blessed—and at times burdened—with a unique frequency that carries a high risk of Overclocking, her forte allows her to quite literally bring her two-dimensional paintings to life as volumetric illusions. She struggles with her intense introversion but pushes past her anxiety to connect with the world around her, using her mystical canvas to aid others and paint a brighter reality.",
                youtubeId: "https://www.youtube.com/watch?v=GPaHYat18DI",
            }
        ]
    }
];

document.addEventListener('DOMContentLoaded', () => {
    buildWikiStructure();
    initializeArchiveFilter();
    initializeSpotlight();
    initializeFatesButton();
    setupModalEvents();
    initializeThemeMode();
    initializeTypewriter();
    initializeCardSparkles();
    injectFactionCrests();
});

function buildWikiStructure() {
    const navContainer = document.getElementById('archive-nav-container');
    const mainContent = document.getElementById('wiki-main-content');

    if (!navContainer || !mainContent) return;

    navContainer.innerHTML = `<button class="nav-btn active" data-filter="all">All Chronicles</button>`;

    wikiDatabase.forEach((fandom, fandomIndex) => {
        const button = document.createElement('button');
        button.className = 'nav-btn';
        button.setAttribute('data-filter', fandom.categoryId);
        button.textContent = fandom.fandomName;
        navContainer.appendChild(button);

        const section = document.createElement('section');
        section.className = 'wiki-section animate-slide-up';
        section.setAttribute('data-category', fandom.categoryId);
        section.style.animationDelay = `${fandomIndex * 0.15}s`;

        let cardsHTML = '';
        fandom.characters.forEach((char, charIndex) => {
            const mediaHTML = char.image && char.image.trim() !== "" 
                ? `<img src="${char.image}" alt="${char.name}" class="char-image" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                   <div class="char-placeholder" style="display:none;"><span>${char.initials}</span></div>`
                : `<div class="char-placeholder"><span>${char.initials}</span></div>`;

            cardsHTML += `
                <article class="char-card" data-fandom-idx="${fandomIndex}" data-char-idx="${charIndex}">
                    <div class="char-image-container">
                        ${mediaHTML}
                    </div>
                    <div class="char-details">
                        <h3 class="char-name">${char.name}</h3>
                        <div class="meta-list">
                            <div class="meta-item">
                                <span class="meta-label">Origin</span>
                                <span class="meta-value">${char.origin}</span>
                            </div>
                            <div class="meta-item">
                                <span class="meta-label">Birthday</span>
                                <span class="meta-value">${char.birthday}</span>
                            </div>
                            <div class="meta-item">
                                <span class="meta-label">Faction</span>
                                <span class="meta-value">${char.faction}</span>
                            </div>
                        </div>
                        <p class="char-summary">${char.summary}</p>
                    </div>
                </article>
            `;
        });

        section.innerHTML = `
            <h2 class="section-title">${fandom.fandomName}</h2>
            <div class="character-grid">
                ${cardsHTML}
            </div>
        `;

        mainContent.appendChild(section);
    });

    const cards = document.querySelectorAll('.char-card');
    cards.forEach(card => {
        card.addEventListener('click', () => {
            const fandomIdx = card.getAttribute('data-fandom-idx');
            const charIdx = card.getAttribute('data-char-idx');
            const characterData = wikiDatabase[fandomIdx].characters[charIdx];
            openModal(characterData);
        });
    });
}

function initializeArchiveFilter() {
    const navButtons = document.querySelectorAll('.nav-btn');
    const sections = document.querySelectorAll('.wiki-section');

    navButtons.forEach(button => {
        button.addEventListener('click', () => {
            const filterValue = button.getAttribute('data-filter');

            navButtons.forEach(btn => btn.classList.remove('active'));
            button.classList.add('active');

            sections.forEach(section => {
                section.classList.remove('animate-slide-up');

                const category = section.getAttribute('data-category');
                const isMatch = filterValue === 'all' || category === filterValue;
                
                const isCurrentlyHidden = section.style.display === 'none';

                if (isMatch) {
                    if (isCurrentlyHidden) {
                        section.style.display = 'block';
                        section.style.opacity = '0';
                        section.style.transform = 'translateY(12px)';
                        
                        section.offsetHeight;
                        setTimeout(() => {
                            section.style.opacity = '1';
                            section.style.transform = 'translateY(0)';
                        }, 20);
                    } else {
                        section.style.display = 'block';
                        section.style.opacity = '1';
                        section.style.transform = 'translateY(0)';
                    }
                } else {
                    section.style.display = 'none';
                }
            });
        });
    });
}

function extractYoutubeId(url) {
    if (!url) return '';
    const cleanUrl = url.trim();
    const regExp = /^.*(youtu.be\/|v\/|u\/\w\/|embed\/|watch\?v=|\&v=|shorts\/|live\/)([^#\&\?]*).*/;
    const match = cleanUrl.match(regExp);
    if (match && match[2] && match[2].length === 11) {
        return match[2];
    }
    return cleanUrl;
}

function openModal(data) {
    const modal = document.getElementById('char-modal');
    const modalContent = document.getElementById('modal-body-content');
    if (!modal || !modalContent) return;

    const mediaHTML = data.image && data.image.trim() !== "" 
        ? `<img src="${data.image}" alt="${data.name}" class="modal-image" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
           <div class="modal-placeholder" style="display:none;"><span>${data.initials}</span></div>`
        : `<div class="modal-placeholder"><span>${data.initials}</span></div>`;

    let videoHTML = '';
    if (data.youtubeId) {
        const parsedId = extractYoutubeId(data.youtubeId);

        videoHTML = `
            <div class="modal-video-section">
                <h4 class="modal-video-title">Archival Footage</h4>
                <div class="modal-video-container">
                    <iframe 
                        src="https://www.youtube.com/embed/${parsedId}" 
                        title="${data.name} Visual Record" 
                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                        referrerpolicy="strict-origin-when-cross-origin"
                        allowfullscreen>
                    </iframe>
                </div>
            </div>
        `;
    }

    modalContent.innerHTML = `
        <div class="modal-header-split">
            <div class="modal-image-wrapper">
                ${mediaHTML}
            </div>
            <div class="modal-meta-info">
                <h3 class="modal-title">${data.name}</h3>
                <div class="meta-list" style="border-bottom: none; margin-bottom: 0; padding-bottom: 0; width: 100%;">
                    <div class="meta-item">
                        <span class="meta-label">Origin</span>
                        <span class="meta-value">${data.origin}</span>
                    </div>
                    <div class="meta-item">
                        <span class="meta-label">Birthday</span>
                        <span class="meta-value">${data.birthday}</span>
                    </div>
                    <div class="meta-item">
                        <span class="meta-label">Faction</span>
                        <span class="meta-value">${data.faction}</span>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="modal-story-section">
            <h4 class="modal-story-title">Biography & Chronicles</h4>
            <p class="modal-story-text">${data.story || data.summary}</p>
        </div>

        ${videoHTML}
    `;

    modal.classList.add('active');
    modal.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
}

function closeModal() {
    const modal = document.getElementById('char-modal');
    const modalContent = document.getElementById('modal-body-content');
    if (!modal) return;

    modal.classList.remove('active');
    modal.setAttribute('aria-hidden', 'true');
    document.body.style.overflow = '';

    if (modalContent) {
        const iframe = modalContent.querySelector('iframe');
        if (iframe) {
            iframe.src = '';
        }
    }
}

function setupModalEvents() {
    const modal = document.getElementById('char-modal');
    const closeBtn = document.getElementById('modal-close-btn');

    if (!modal || !closeBtn) return;

    closeBtn.addEventListener('click', closeModal);

    modal.addEventListener('click', (e) => {
        if (e.target === modal) {
            closeModal();
        }
    });

    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && modal.classList.contains('active')) {
            closeModal();
        }
    });
}

function initializeSpotlight() {
    const spotlightContainer = document.getElementById('spotlight-container');
    if (!spotlightContainer) return;

    const allCharacters = [];
    wikiDatabase.forEach(fandom => {
        fandom.characters.forEach(char => {
            allCharacters.push(char);
        });
    });

    if (allCharacters.length === 0) return;

    const randomIndex = Math.floor(Math.random() * allCharacters.length);
    const randomChar = allCharacters[randomIndex];

    const mediaHTML = randomChar.image && randomChar.image.trim() !== "" 
        ? `<img src="${randomChar.image}" alt="${randomChar.name}" class="spotlight-image" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
           <div class="spotlight-placeholder" style="display:none;"><span>${randomChar.initials}</span></div>`
        : `<div class="spotlight-placeholder"><span>${randomChar.initials}</span></div>`;

    spotlightContainer.innerHTML = `
        <div class="spotlight-banner animate-slide-up" style="animation-delay: 0.1s;">
            <div class="spotlight-badge">Archival Spotlight</div>
            <div class="spotlight-content">
                <div class="spotlight-image-container">
                    ${mediaHTML}
                </div>
                <div class="spotlight-details">
                    <h2 class="spotlight-name">${randomChar.name}</h2>
                    <div class="spotlight-meta">
                        <span class="spotlight-meta-item"><strong>Origin:</strong> ${randomChar.origin}</span>
                        <span class="spotlight-meta-divider">•</span>
                        <span class="spotlight-meta-item"><strong>Faction:</strong> ${randomChar.faction}</span>
                    </div>
                    <p class="spotlight-summary">${randomChar.summary}</p>
                    <button class="spotlight-btn" aria-label="Open chronicle for ${randomChar.name}">
                        Read Chronicle <span>✦</span>
                    </button>
                </div>
            </div>
        </div>
    `;
    
    const banner = spotlightContainer.querySelector('.spotlight-banner');
    if (banner) {
        banner.addEventListener('click', () => {
            openModal(randomChar);
        });
    }
}

function initializeFatesButton() {
    const fatesBtn = document.getElementById('fates-button');
    if (!fatesBtn) return;

    fatesBtn.addEventListener('click', () => {
        const allCharacters = [];
        wikiDatabase.forEach(fandom => {
            fandom.characters.forEach(char => {
                allCharacters.push(char);
            });
        });

        if (allCharacters.length === 0) return;

        const randomIndex = Math.floor(Math.random() * allCharacters.length);
        const randomChar = allCharacters[randomIndex];

        fatesBtn.style.transform = 'scale(0.9) rotate(4deg)';
        
        setTimeout(() => {
            fatesBtn.style.transform = '';
            openModal(randomChar);
        }, 120);
    });
}

function initializeThemeMode() {
    const toggleBtn = document.getElementById('theme-toggle');
    if (!toggleBtn) return;

    let savedTheme = null;
    try {
        savedTheme = localStorage.getItem('fachi-theme');
    } catch (e) {
    }

    const currentHour = new Date().getHours();
    const isNightTime = currentHour >= 19 || currentHour < 6; 

    if (savedTheme === 'dark' || (!savedTheme && isNightTime)) {
        document.body.classList.add('dark-mode');
        toggleBtn.textContent = '☾';
    } else {
        document.body.classList.remove('dark-mode');
        toggleBtn.textContent = '☼';
    }

    toggleBtn.addEventListener('click', () => {
        const isDarkNow = document.body.classList.toggle('dark-mode');
        toggleBtn.textContent = isDarkNow ? '☾' : '☼';
        try {
            localStorage.setItem('fachi-theme', isDarkNow ? 'dark' : 'light');
        } catch (e) {
        }
    });
}

function initializeTypewriter() {
    const taglineElement = document.getElementById('hero-tagline-text');
    if (!taglineElement) return;

    const textToType = taglineElement.textContent.trim();
    taglineElement.textContent = ''; 
    taglineElement.style.opacity = '1';

    let charIdx = 0;
    function typeChar() {
        if (charIdx < textToType.length) {
            taglineElement.textContent += textToType.charAt(charIdx);
            charIdx++;
            
            const lastChar = textToType.charAt(charIdx - 1);
            const delay = lastChar === ',' || lastChar === '.' ? 240 : 35 + Math.random() * 35;
            setTimeout(typeChar, delay);
        }
    }
    
    setTimeout(typeChar, 400);
}

function initializeCardSparkles() {
    const mainContent = document.getElementById('wiki-main-content');
    if (!mainContent) return;

    let lastSpawn = 0;
    const throttleThreshold = 100; 

    mainContent.addEventListener('mousemove', (e) => {
        const activeCard = e.target.closest('.char-card');
        if (!activeCard) return;

        const timestamp = Date.now();
        if (timestamp - lastSpawn < throttleThreshold) return;
        lastSpawn = timestamp;

        for (let i = 0; i < 2; i++) {
            createParticle(e.clientX, e.clientY);
        }
    });

    function createParticle(cursorX, cursorY) {
        const element = document.createElement('span');
        element.className = 'sparkle-particle';
        element.textContent = '✦';

        const randomOffsetX = (Math.random() - 0.5) * 16;
        const randomOffsetY = (Math.random() - 0.5) * 8;
        
        element.style.left = `${cursorX + randomOffsetX + window.scrollX}px`;
        element.style.top = `${cursorY + randomOffsetY + window.scrollY}px`;

        const sizeScale = 0.5 + Math.random() * 0.7;
        element.style.fontSize = `${sizeScale}rem`;

        const driftX = (Math.random() - 0.5) * 35;
        const driftY = -35 - Math.random() * 25; 
        element.style.setProperty('--move-x', `${driftX}px`);
        element.style.setProperty('--move-y', `${driftY}px`);

        document.body.appendChild(element);

        setTimeout(() => {
            element.remove();
        }, 500);
    }
}

function injectFactionCrests() {
    const cards = document.querySelectorAll('.char-card');
    cards.forEach(card => {
        const factionNode = Array.from(card.querySelectorAll('.meta-item')).find(item => {
            const label = item.querySelector('.meta-label');
            return label && label.textContent.toLowerCase().includes('faction');
        });

        const factionValue = factionNode ? factionNode.querySelector('.meta-value').textContent.trim() : '';
        const crestSymbol = getFactionCrestSymbol(factionValue);

        const crestElement = document.createElement('div');
        crestElement.className = 'faction-crest';
        crestElement.textContent = crestSymbol;
        card.appendChild(crestElement);
    });
}

function getFactionCrestSymbol(faction) {
    const lowerFaction = faction.toLowerCase();
    if (lowerFaction.includes('spider') || lowerFaction.includes('stellaron')) return '🕸';
    if (lowerFaction.includes('index') || lowerFaction.includes('rhodes')) return '☩';
    if (lowerFaction.includes('scientific') || lowerFaction.includes('montelli')) return '⚙';
    return '✦';
}
